#ifndef SELECTION_SORT
#define SELECTION_SORT

#include <vector>
#include "basic_function.h"

void selection_sort(int array[], int size);     // sort array
void selection_sort(std::vector<int> &vector);  // sort vector

#endif